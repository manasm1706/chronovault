<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="?android:attr/colorBackground"
    tools:context=".ui.capsules.CapsulesFragment">

    <!-- Filter Chips -->
    <HorizontalScrollView
        android:id="@+id/scroll_filters"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:scrollbars="none"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:padding="12dp">

            <com.google.android.material.chip.Chip
                android:id="@+id/chip_all"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/filter_all"
                android:layout_marginEnd="8dp"
                app:chipBackgroundColor="?attr/colorPrimary"
                android:textColor="?attr/colorOnPrimary" />

            <com.google.android.material.chip.Chip
                android:id="@+id/chip_locked"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/filter_locked"
                android:layout_marginEnd="8dp"
                app:chipBackgroundColor="?attr/colorSurface"
                app:chipStrokeColor="?attr/colorOutline"
                app:chipStrokeWidth="1dp" />

            <com.google.android.material.chip.Chip
                android:id="@+id/chip_unlocked"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/filter_unlocked"
                android:layout_marginEnd="8dp"
                app:chipBackgroundColor="?attr/colorSurface"
                app:chipStrokeColor="?attr/colorOutline"
                app:chipStrokeWidth="1dp" />

            <com.google.android.material.chip.Chip
                android:id="@+id/chip_shared"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginEnd="8dp"
                android:text="@string/filter_shared"
                app:chipBackgroundColor="?attr/colorSurface"
                app:chipStrokeColor="?attr/colorOutline"
                app:chipStrokeWidth="1dp" />

            <com.google.android.material.chip.Chip
                android:id="@+id/chip_personal"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginEnd="8dp"
                android:text="@string/filter_personal"
                app:chipBackgroundColor="?attr/colorSurface"
                app:chipStrokeColor="?attr/colorOutline"
                app:chipStrokeWidth="1dp" />

            <com.google.android.material.chip.Chip
                android:id="@+id/chip_public"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/filter_public"
                app:chipBackgroundColor="?attr/colorSurface"
                app:chipStrokeColor="?attr/colorOutline"
                app:chipStrokeWidth="1dp" />
        </LinearLayout>
    </HorizontalScrollView>

    <!-- Capsules RecyclerView -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rv_capsules"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:padding="8dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/scroll_filters"
        tools:listitem="@layout/item_capsule" />

    <!-- Loading Progress -->
    <ProgressBar
        android:id="@+id/progress_capsules"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:visibility="gone"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <!-- FAB Create Capsule -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fab_create_capsule"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="16dp"
        android:contentDescription="@string/button_create_capsule"
        android:src="@drawable/ic_asset_bookmark"
        android:scaleType="centerInside"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />
</androidx.constraintlayout.widget.ConstraintLayout>

